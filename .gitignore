node_modules/
.next/
.env
.env.local
.env*.local

# build / cache
out/
.vercel/
*.tsbuildinfo

# logs
*.log
npm-debug.log*

# os / editor
.DS_Store
Thumbs.db

# replit runtime
.upm/
.cache/

# local scratch scripts
scratch/

# Supabase CLI cache
supabase/.temp/

# Written by `next dev` on every start, from
# node_modules/next/dist/server/lib/generate-agent-files.js. Not authored here,
# and not instructions this project has adopted — deleting them only makes the
# next dev run recreate them, so they are ignored rather than committed.
AGENTS.md
CLAUDE.md

# The ten-dataset evaluation's generated inputs and captured app output.
# `node eval/datasets.mjs` rebuilds the CSVs byte-for-byte; the captures are
# large and reproducible, so only the harness and the write-up are tracked.
eval/data/
eval/app-output/
